options {
    directory "/var/cache/bind";

    recursion no;                  # Ne pas répondre aux requêtes récursives
    allow-query { any; };          # Autoriser les requêtes de n'importe où
    listen-on { any; };            # Écoute sur toutes les interfaces réseau
};
