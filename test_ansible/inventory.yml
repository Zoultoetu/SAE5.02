all:
  children:
    docker_host:
      hosts:
        localhost:
          ansible_connection: local
    bind9_container:
      hosts:
        bind9:
          ansible_host: 127.0.0.1
          ansible_port: 2222
          ansible_user: root
          ansible_ssh_pass: root
